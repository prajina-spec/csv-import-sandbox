# csv-import-sandbox

# Approach CSV Import Sandbox

Simple, no-database CSV import tool with field mapping, validation, and downloadable output files.

## How to run:

### Backend
```bash
cd backend
npm install
npm start

Backend will run on http://localhost:4000

cd frontend
npm install
npm start


Frontend will run on http://localhost:3000


---

# ✅ You Are DONE!

Now once you've committed these last three files, your GitHub repo is **fully ready** to clone locally and run!  
(Backend + Frontend will npm install + start separately.)

---

# 🚀 Next for you:

After all files are pushed:
```bash
# Open two terminals:
cd backend
npm install
npm start

cd frontend
npm install
npm start
